{
  "name": "elromco-inventory-bot",
  "version": "1.0.0",
  "description": "Playwright bot that fills inventory in Elromco CRM",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-browsers": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "playwright": "^1.44.0",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
